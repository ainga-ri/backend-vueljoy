spring:
  datasource:
    url: ***
    username: ***
    password: ****
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  profiles:
    active: load-data
server:
  port: 8080
  address: 0.0.0.0
